<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aechkae.organizer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".focus.FocusActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".focus.SettingsActivity" />
        <activity android:name=".timer.TimerActivity" />
        <activity android:name=".focus.AddTaskActivity" android:noHistory="true"/>
        <activity android:name=".reminder.ReminderActivity" />
        <activity android:name=".focus.HowToUseActivity" />
        <activity android:name=".notable.NotableActivity"/>
    </application>

</manifest>