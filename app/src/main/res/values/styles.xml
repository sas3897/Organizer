<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/focusColorPrimary</item>
        <item name="android:colorPrimaryDark">@color/focusColorPrimaryDark</item>
        <item name="android:colorAccent">@color/focusColorAccent</item>
    </style>

</resources>
